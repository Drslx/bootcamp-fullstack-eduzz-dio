//
const arr = [1, 3, 2, 4, 6, 5];
// Retorno  sem ordem
console.log(arr);

// Retorno com  ordem
console.log(arr.sort());

const list = [10, 1, 5, 9, 8, 12, 15];

console.log(list);

console.log(list.sort());
