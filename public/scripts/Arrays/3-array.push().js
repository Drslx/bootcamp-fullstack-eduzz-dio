// push adiciona um novo elemento no final do array e retorna o tamanho da lista
const arr = ["Doug", "Thaty"];
// Antes do push
console.log(arr);

const newArr = arr.push("Fred", "Krueger");
// Novo array com a quantidade
console.log(newArr);

// Depois do push
console.log(arr);
